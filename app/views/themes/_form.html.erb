<%= simple_form_for(@theme) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :photo, as: :file %>
  <% submit_label = @theme.new_record? ? "Create File" : "Update File" %>
  <%= f.button :submit, submit_label, class: "create-btn" %>
<% end %>
