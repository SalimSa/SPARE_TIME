<div class="container mt-4">
  <div class="row">
    <div class="col-6 m-auto">
      <div class="theme-form">
        <h1>Edit file</h1>
        <div class="mt-3">
          <%= render "form", theme: @theme %>
          <%= link_to "Delete File", theme_path(@theme), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, :class => "delete-file"  %>
        </div>
      </div>
    </div>
  </div>
</div>
